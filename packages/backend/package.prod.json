{
  "name": "@oscarr/backend-runtime",
  "private": true,
  "type": "module",
  "description": "Minimal runtime manifest for the bundled prod image — only externals esbuild left out (native + Prisma). No version field: read from root package.json.",
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "prisma": "^6.19.3",
    "bcrypt": "^6.0.0",
    "bare-fs": "^4.7.1",
    "bare-os": "^3.9.0",
    "bare-path": "^3.0.0",
    "bare-url": "^2.4.2"
  },
  "overrides": {
    "picomatch": ">=4.0.4"
  }
}
