import { createMediaServerProvider } from '../mediaServerProvider.js';

export const jellyfinProvider = createMediaServerProvider({ id: 'jellyfin', label: 'Jellyfin', icon: '/providers/jellyfin.svg' });
